{
  "sessdata": {
    "description": "B站 SESSDATA Cookie（用于绕过反爬，推荐配置）",
    "type": "string",
    "hint": "登录B站后，F12 → Application → Cookies → bilibili.com → SESSDATA",
    "obvious_hint": true
  },
  "default_keyword": {
    "description": "默认搜索关键词",
    "type": "string",
    "default": "杀戮尖塔2"
  },
  "order": {
    "description": "排序方式",
    "type": "string",
    "default": "pubdate",
    "options": ["pubdate", "click", "stow"],
    "hint": "pubdate: 按发布时间排序 | click: 按播放量排序 | stow: 按收藏数排序"
  },
  "target_count": {
    "description": "目标数量（集满模式）或最大获取数量（普通模式）",
    "type": "int",
    "default": 10,
    "hint": "集满模式：需要收集的满足条件的视频数量；普通模式：最大获取数量"
  },
  "use_collect_mode": {
    "description": "使用集满模式",
    "type": "bool",
    "default": true,
    "hint": "True: 持续搜索直到满足条件的视频达到目标数量；False: 获取固定数量后筛选"
  },
  "default_max_count": {
    "description": "普通模式最大获取数量",
    "type": "int",
    "default": 50,
    "hint": "仅在普通模式下生效"
  },
  "tiered_filter": {
    "description": "启用分层筛选（根据视频年龄动态调整阈值）",
    "type": "bool",
    "default": true,
    "hint": "开启后：5小时内>2000/h, 5-24小时>1500/h, 24小时以上>1000/h"
  },
  "tier_5h_threshold": {
    "description": "5小时以内视频的阈值（每小时播放量）",
    "type": "float",
    "default": 2000.0
  },
  "tier_24h_threshold": {
    "description": "5-24小时视频的阈值（每小时播放量）",
    "type": "float",
    "default": 1500.0
  },
  "tier_old_threshold": {
    "description": "24小时以上视频的阈值（每小时播放量）",
    "type": "float",
    "default": 1000.0
  },
  "export_json": {
    "description": "是否导出JSON文件",
    "type": "bool",
    "default": false
  },
  "enable_analysis": {
    "description": "启用AI总结功能",
    "type": "bool",
    "default": true,
    "hint": "在命令中输入「总结」或「分析」关键词时触发"
  },
  "analysis_prompt": {
    "description": "AI总结提示词（给LLM的指令）",
    "type": "string",
    "default": "请简要总结这些视频的特点和内容，推荐一些高质量的视频，并分析当前的热门趋势",
    "hint": "这是给AI的指令，可以自定义总结的风格和内容"
  }
}
